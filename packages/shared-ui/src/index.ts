// shadcn/ui components are copied into this package (not an npm dependency)
// as each is needed, starting when the web dashboard shell (Phase 3) and
// desktop till (Phase 4) are built — so both apps share verbatim components.
// Intentionally empty until then.
export {};
