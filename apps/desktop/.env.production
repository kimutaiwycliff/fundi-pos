# Real production domains, baked into every distributed build (Windows/Mac
# installers via .github/workflows/desktop-release.yml). Not secret - these
# are the same public URLs every till and browser already connects to.
VITE_API_BASE_URL=https://api.fundipos.co.ke
VITE_POWERSYNC_URL=https://sync.fundipos.co.ke
